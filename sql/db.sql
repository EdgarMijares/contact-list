CREATE TABLE IF NOT EXISTS users (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,name VARCHAR(60),email VARCHAR(40),password TEXT);
CREATE TABLE IF NOT EXISTS contacts (id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,name VARCHAR(60),email VARCHAR(40),phone VARCHAR(20),userId INTEGER REFERENCES users(id));

